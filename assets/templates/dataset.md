# DataSet

> 每个数据集下完立刻补一段。**Agent 靠这份判断能不能直接用，别让它自己猜目录结构。**

## 总览

| 数据集 | 用途 | 状态 | 大小 |
|---|---|---|---|
| {{}} | | ✅ 就绪 / ⚠️ 有坑 | |

## {{数据集名}}

**目录结构**：`{{路径}}/...`，每条记录长这样：`{{格式}}`

**处理过程**：{{下载后做了什么。去重、格式转换、过滤}}

**⚠️ 坑**：{{有的话写这里。比如某部分有重复条目，去重前后指标差多少}}

```bash
{{处理脚本的调用方式}}
```

## split

```
train: {{}}
test:  {{}}
```

**按 {{维度}} 分，不按样本分。**{{理由}}

## 下数据的两个命令

```bash
caffeinate -i          # Mac 不休眠，否则合盖/息屏就断连
```

```
/loop 下载 {{X}} 到 {{Y}}，停止条件是校验通过，断了自动续传
```
