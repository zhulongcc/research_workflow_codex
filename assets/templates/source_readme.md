# Paper source

这里是论文 LaTeX 和附图，不是模型代码；使用与选定 PDF 对应的同一版本。
下载后沿用原仓库的清洗阅读方式：`.tex` 剥注释，原文留 `.orig`，疑似旧稿目录隔离到 `.stale/`。
`raw/` 和 `source.archive` 保留未经修改的同版材料；清洗副本只辅助阅读，公式与数字对照 PDF。
只记来源、版本和日期，不记录 SHA256；没有源码就说明。
