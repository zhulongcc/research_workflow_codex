# research_workflow_codex 项目规则

本仓库用于持续优化用户自己的 research workflow，以用户明确表达的主观标准为准。

## 适用范围与每次开始

- 本文件只约束本仓库及其子目录，以及在本项目中工作的主代理、子代理和后继对话；不修改全局规则，也不自动推广到其他项目。
- 每个新对话、恢复的对话和后继对话开始工作前，必须先读本文件，再读该任务对应的交接记录；上下文压缩后也必须恢复这些约束。
- `assets/templates/` 是供其他项目使用的模板素材，其中的会话规则不能替代本项目规则。未经用户要求，不把本文件中的个人约束复制到分发模板。
- 本文件记录用户的持续授权与限制；除非用户明确更新，不得由代理自行放宽。系统和开发者指令仍优先适用。

## 1. Git：允许本地提交，远端写入必须获同意

- 允许按任务需要进行本地编辑、暂存、创建分支和本地 `git commit`，无须为本地提交另行询问。
- **未经用户明确同意，禁止向 Git 远端提交或写入更改。** 包括 `git push`、推送分支或标签、强推、删除远端引用，以及通过 API、网页或其他工具直接修改远端代码、创建远端提交或合并 PR。
- “完成任务”“修好它”“可以提交”等未明确涉及远端的表述，不视为推送授权。不得通过脚本、自动化或子代理绕过限制。
- 已获得的远端授权在其明确范围内有效，不重复询问；不能把某次推送的同意扩展为其他任务或所有未来推送的同意。

## 2. 每个对话最多 8 次上下文自动压缩

### 计数与记录

- 上限按**每个独立对话**计算，计数对象是已发生的上下文自动压缩，不是用户消息数、工具调用数、摘要次数或子代理数量。
- 确认是全新且无继承历史的对话时从 `0` 开始；恢复同一对话必须沿用计数，不能因新一轮用户消息而清零。
- 以运行时明确的压缩事件、可核实的本对话日志或可信交接计数为依据；同一事件只计一次。不能把手动压缩当成已确认的自动压缩，也不能用手动压缩重置计数。
- 发生压缩或准备交接时，主代理维护 `.workflow/sessions/<对话标识>.md`，记录对话标识、自动压缩次数、计数依据、最近已计入事件、任务状态及交接路径。若无法取得对话 ID，生成唯一标识并在后续摘要中沿用；不同对话不得共用一份计数文件。
- 每次压缩摘要都应保留计数、对应状态文件路径和本文件路径。恢复后先核对再继续，避免重复计数。
- 若平台未暴露可靠事件、历史计数丢失或无法区分压缩类型，明确标记“未知”及已确认下限，禁止猜测、伪报为 `0` 或声称能精确预测下一次压缩。先尝试核实；仍无法核实时告知用户，并在上下文再次紧张前保守地提前交接。

### 第 7 次：提前提醒

- 确认完成第 **7** 次自动压缩时，立即提醒用户：“本对话已自动压缩 7 次，接近 8 次上限，需要准备开启新的子对话。”
- 提前整理交接记录，优先完成当前可安全收尾的小步骤，避免启动难以中断的长流程。若平台明确提示第 8 次压缩即将发生，则在压缩前再次提醒。

### 第 8 次：中断并自动续开

确认达到第 **8** 次（或发现已超过）后，主代理停止在旧对话中推进实质任务；只执行必要的安全收尾、状态保存和交接，不等待第 9 次。

1. 告知用户已达到上限，正在整理并转交任务。收集子代理进度，让正在写文件的代理在安全点交还工作；记录仍运行的进程或实验，不擅自终止它们。
2. 写入 `.workflow/handoffs/<原对话标识>.md`，至少包含：原始目标、用户偏好和约束、已获得的授权及范围、已完成与未完成事项、关键决策、修改文件、验证结果、阻塞点、后台进程和子代理状态、工作目录与分支、未提交更改、明确的下一步。记录源对话的压缩次数，但不要将其作为新对话的计数。
3. **用户已预先授权本条件下自动创建新的子对话并继续执行，无须再次确认。** 使用当前可用的任务创建工具，在同一个项目、同一工作目录开启具有干净上下文的后继对话，以便接续未提交更改；不要通过复制完整旧历史的 fork 假装清空上下文，也不要把并行子代理当成后继对话。
4. 新对话的启动提示必须写明：这是哪个任务的续接、项目与工作目录、先读取项目根 `AGENTS.md` 和本次交接文件、继续哪些未完成工作、沿用哪些授权与限制。新对话从 `0` 开始独立计数，且继续遵守全部项目规则。
5. 创建成功后，将返回的新对话标识写回源对话状态和交接记录，并向用户提供入口。检查新对话是否已开始接手；旧对话随后停止执行原任务，避免两个对话重复修改同一工作区。
6. 一次交接只能创建一个后继对话。若创建结果不明确，先查询是否已创建，禁止盲目重试。若任务创建工具不可用或创建失败，保存交接文件和可直接使用的续接提示，说明阻塞并请用户手动开启；不得假装已经续开，也不得在旧对话中继续消耗上下文推进任务。

### 能力边界

本文件是代理执行规则，不能单独为 Codex 安装压缩事件监听器、设置应用层面的“最多 8 次”硬限制，或保证平台一定在压缩前通知代理。只有在事件可观测且任务创建工具可用时，才能自动完成精确计数与续接；能力不足时必须按上述保守交接流程处理。

## 3. Multi-agent 协作

- 允许主代理自主使用 multi-agents；能提高质量或缩短执行时间时，按清晰、可独立完成的子任务并行分工，无须逐次征求同意。
- 分派任务时传递本项目规则、相关上下文及文件修改边界，避免多个代理同时改同一文件。
- 所有子代理同样受远端写入限制约束。主代理负责整合结果、必要验证以及向用户汇报。
- 主代理统一负责所属用户对话的计数和后继对话创建。子代理的压缩不累加到主对话；子代理自身到达上限时停止工作、整理交接并通知主代理，由主代理替换执行者，禁止各自创建用户侧后继对话。
